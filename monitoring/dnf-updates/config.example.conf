# Strict KEY=value file. Values are literal; shell expansion is not evaluated.

# CUSTOMIZE: Set the textfile path. It must end in .prom and sit in the folder named by node_exporter's --collector.textfile.directory.
OUTPUT_FILE=/var/lib/prometheus/node-exporter/dnf.prom
# CUSTOMIZE: Set the package that provides your running kernel, such as kernel or kernel-core.
KERNEL_PACKAGE=kernel
