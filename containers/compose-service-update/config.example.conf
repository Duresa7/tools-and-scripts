# Strict KEY=value file. Values are literal; shell expansion is not evaluated.

# CUSTOMIZE: Name of the running container to inspect for Compose configuration.
CONTAINER_NAME=web-service-1
# CUSTOMIZE: Compose project name passed to docker compose.
PROJECT_NAME=web
# CUSTOMIZE: Compose service name to pull and recreate.
SERVICE_NAME=web
# CUSTOMIZE: Absolute directory path that all Compose definition files must reside within.
ALLOWED_COMPOSE_ROOT=/srv/compose
