* text=auto

# A bash script with CRLF fails on Linux and macOS with "\r: command not found".
*.sh   text eol=lf

# PowerShell is happiest with CRLF on Windows and tolerates it elsewhere.
*.ps1  text eol=crlf

*.md   text
*.yaml text
*.svg  text
